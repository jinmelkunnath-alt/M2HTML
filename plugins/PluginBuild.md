# Plugin Build

Plugins are self-contained C modules compiled against include/markforge/markforge.h. Prompt 3 supports static registration and manifest discovery; dynamic loading can be added without changing the SDK structures.
