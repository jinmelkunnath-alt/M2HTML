import { ReactNode } from 'react';
import { motion } from 'framer-motion';
export function Button({ children, onClick, variant = 'primary', ariaLabel }: { children: ReactNode; onClick?: () => void; variant?: 'primary' | 'ghost' | 'danger'; ariaLabel?: string }) { const cls = variant === 'primary' ? 'bg-forge-500 text-white hover:bg-forge-400' : variant === 'danger' ? 'bg-red-500 text-white' : 'bg-white/10 hover:bg-white/15'; return <button aria-label={ariaLabel} onClick={onClick} className={`focus-ring rounded-xl px-4 py-2 text-sm font-semibold transition ${cls}`}>{children}</button>; }
export function Card({ children, className = '' }: { children: ReactNode; className?: string }) { return <motion.section initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className={`rounded-3xl border border-white/10 bg-white/10 p-6 shadow-glow backdrop-blur ${className}`}>{children}</motion.section>; }
export function Tabs({ tabs, active, onChange }: { tabs: string[]; active: string; onChange: (tab: string) => void }) { return <div className="flex flex-wrap gap-2" role="tablist">{tabs.map(t => <button key={t} role="tab" aria-selected={active === t} onClick={() => onChange(t)} className={`rounded-xl px-3 py-2 text-sm ${active === t ? 'bg-forge-500 text-white' : 'bg-white/10'}`}>{t}</button>)}</div>; }
export function Panel({ title, children }: { title: string; children: ReactNode }) { return <div className="rounded-2xl border border-white/10 bg-black/20 p-4"><h3 className="mb-3 text-sm font-bold uppercase tracking-wider text-forge-400">{title}</h3>{children}</div>; }
export function Skeleton() { return <div className="animate-pulse rounded-2xl bg-white/10 h-32" aria-label="Loading" />; }
export function Notification({ children }: { children: ReactNode }) { return <div role="status" className="fixed bottom-5 right-5 rounded-2xl bg-forge-500 px-4 py-3 text-white shadow-glow">{children}</div>; }
