import { Command } from '../hooks/useCommandPalette';
export function CommandPalette({ open, query, setQuery, commands, close }: { open: boolean; query: string; setQuery: (q: string) => void; commands: Command[]; close: () => void }) { if (!open) return null; return <div className="fixed inset-0 z-50 bg-black/60 p-6" role="dialog" aria-modal="true"><div className="mx-auto max-w-2xl rounded-3xl border border-white/10 bg-slate-950 p-4 shadow-glow"><input autoFocus value={query} onChange={e => setQuery(e.target.value)} aria-label="Search commands" className="w-full rounded-2xl bg-white/10 px-4 py-3 outline-none" /> <div className="mt-3 max-h-96 overflow-auto">{commands.map(c => <button key={c.id} className="block w-full rounded-xl px-4 py-3 text-left hover:bg-white/10" onClick={() => { c.run(); close(); }}>{c.label}</button>)}</div></div></div>; }
