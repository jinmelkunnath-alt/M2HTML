import { Metrics } from '../wasm/markforgeWasm';
export function PerformanceDashboard({ metrics }: { metrics?: Metrics }) { const items = metrics ? [['Parse', `${metrics.parseTime.toFixed(2)}ms`], ['Render', `${metrics.renderTime.toFixed(2)}ms`], ['Characters', metrics.characters], ['Words', metrics.words], ['Lines', metrics.lines], ['Tokens', metrics.tokens], ['AST Nodes', metrics.astNodes], ['Memory', `${metrics.memoryUsage}b`], ['Output', `${metrics.outputSize}b`], ['Mode', metrics.mode]] : []; return <div className="grid grid-cols-2 gap-3 md:grid-cols-5">{items.map(([k,v]) => <div key={k} className="rounded-2xl bg-white/10 p-3"><div className="text-xs opacity-70">{k}</div><div className="font-bold">{v}</div></div>)}</div>; }
