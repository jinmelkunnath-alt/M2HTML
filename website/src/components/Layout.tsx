import { ReactNode } from 'react';
import { Link, NavLink } from './Router';
const pages = ['Home','Playground','Documentation','Examples','Benchmarks','Architecture','Plugins','Downloads','Releases','Roadmap','About','Contributing'];
export function Layout({ children }: { children: ReactNode }) { return <div className="min-h-screen"><a href="#main" className="sr-only focus:not-sr-only">Skip to content</a><nav className="sticky top-0 z-40 border-b border-white/10 bg-ink/80 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link href="/" className="text-xl font-black">⚒ MarkForge</Link><div className="hidden gap-3 lg:flex">{pages.slice(1,8).map(p => <NavLink key={p} href={`/${p.toLowerCase()==='plugins'?'marketplace':p.toLowerCase()}`}>{p}</NavLink>)}</div></div></nav><main id="main" className="mx-auto max-w-7xl px-5 py-8">{children}</main><footer className="mt-20 border-t border-white/10 px-5 py-10 text-center text-sm opacity-75">MarkForge is fully offline, self-contained, and built for developers.</footer></div>; }
