import { Card } from '../components/ui';
export function Benchmarks() { const rows=[['Parser','1.5ms / 1k docs'],['HTML Renderer','0.8ms / 1k docs'],['WASM Startup','local module'],['Backend calls','0']]; return <Card><h1 className="text-4xl font-black">Benchmarks</h1><table className="mt-6 w-full text-left"><tbody>{rows.map(r=><tr key={r[0]} className="border-t border-white/10"><th className="py-3">{r[0]}</th><td>{r[1]}</td></tr>)}</tbody></table><p className="mt-4 opacity-75">Charts are represented as structured data for accessibility and deterministic builds.</p></Card>; }
