# Theme Guide

Themes are alpha key/value records backed by the configuration map. A theme has a name, version, optional parent string and values map.

Theme inheritance, discovery, compatibility validation and schema enforcement are not implemented.
