# Formatting Guide

`mf_format_markdown` currently emits simple normalized Markdown for headings, paragraphs, code blocks, horizontal rules and fallback text nodes.

Advanced formatting features such as table alignment, line wrapping, definition sorting and full idempotent formatting profiles are not implemented in this alpha.
